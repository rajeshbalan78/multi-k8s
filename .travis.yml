sudo: required
services:
  - docker

before_install:
  - docker build -t rajeshbalan78/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true rajeshbalan78/react-test npm test

after_success:
  - docker build -t rajeshbalan/multi-client ./client
  - docker build -t rajeshbalan/multi-nginx ./nginx
  - docker build -t rajeshbalan/multi-server ./server
  - docker build -t rajeshbalan/multi-worker ./worker

  # Login to Docker Hub, the variables are defined in Travis CI
  
  - echo "$DOCKER_PASSWORD" | docker login -u "DOCKER_ID" --password-stdin
 
  # push the production images to docker hub

  - docker push rajeshbalan/multi-client
  - docker push rajeshbalan/multi-nginx
  - docker push rajeshbalan/multi-server
  - docker push rajeshbalan/multi-worker
